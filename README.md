# UnVocal

A service for processing YouTube videos to extract vocals using Demucs.

## Architecture

- **API**: Go service handling HTTP requests
- **Worker**: Python service processing jobs via Redis queue
- **Storage**: File storage for uploads and outputs

## Setup

1. Install Docker and Docker Compose
2. Run `docker-compose up --build`

## Usage

TODO: Add usage instructions

