root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/main ./cmd/server"
bin = "./tmp/main"

include_ext = ["go"]
exclude_dir = [
  "storage",
  "logs",
  ".venv",
  "tmp",
]

delay = 1000

[log]
time = true

[color]
main = "cyan"
watcher = "yellow"
build = "green"
runner = "magenta"